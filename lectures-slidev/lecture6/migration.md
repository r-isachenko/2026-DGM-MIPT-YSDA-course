# Lecture 6: перенос в Slidev

Перенос завершён 2026-09-15 отдельным агентом L6 и общим координатором миграции 5–8. Общий регламент — [MIGRATION.md](../MIGRATION.md), команды — [README.md](../README.md).

## Перенос границ лекций до L7 — 2026-09-26

По запросу автора после возврата части VAE в L3 границы тем сдвинуты раньше вплоть до принимающей L7. Этот раздел описывает текущую редакцию и её завершённые проверки; разделы исходной миграции ниже сохраняют историю 2026-09-15.

- Основная часть L6 теперь начинается со Score Matching; вводные Energy-Based Models и Langevin Dynamics (собственные Beamer frames 13–14) преподаются в L5 и здесь сохранены в Recap под прежними номерами. Их содержимое, формулы, ссылки, изображение и раскрытия не изменены, изменён только заголовок на Recap of Previous Lecture. Раздел Langevin Dynamics внесён в `omittedSourceSections`.
- Входящий Recap сохраняет mode collapse, сравнение дивергенций, Wasserstein/WGAN и evaluation. Старый GAN optimality/Expectations frame 2 исключён (`omittedSourceFrames: [2]`); блок Reality из frame 3 также убран: этот материал теперь находится в основной части L4. Остальные части frame 3 и frames 4–11 сохранены.
- После NCSN перенесён весь Forward Gaussian Diffusion Process из L7: исходные Beamer frames 9–14, включая taxonomy, переходное ядро, связь с Langevin, полный вывод замкнутого условного распределения, предельное нормальное распределение и условие на noise schedule, иллюстрации и сравнение с DSM. Это 10 логических слайдов и один переход Outline. Сохранены четыре исходных продолжения frames 10/11/12/14 с идентификаторами `extension: imported: 7:K`; формулы, шаги раскрытия и ссылки не изменены.
- 16 прежних основных слайдов L6 (frames 15–30) побайтно совпадают с редакцией до переноса. Намеренный повтор DSM frames 21 и 26 сохранён. Training и Sampling (Annealed Langevin Dynamics) не изменены.
- Все Outline обновлены; статический Summary пересобран в пять тезисов о score matching, DSM, NCSN и прямой диффузии. Всего 44 логических слайда, 57 кликов, 101 состояние: 30 сохранённых собственных frames, один разрешённый пропуск, шесть импортированных frames, четыре продолжения и четыре автоматических Outline.
- `conditional_diffusion.png` и `diffusion_over_time.png` скопированы в локальную `public/figs/` без изменений, хеши совпадают с исходными файлами L7. Общая тема, макросы и Beamer не редактировались.
- Выполнено сравнение сохранённых и импортированных слайдов с `/private/tmp/dgm-through7-baseline/`: все перечисленные основные слайды, импортированные слайды и оба EBM/Langevin блока совпадают, кроме новых provenance IDs, комментариев и заголовков Recap. `git diff --check` прошёл.
Проверки текущей редакции завершены: README/Outline, входящий и исходящий Recap, Summary, карты, формулы, раскрытия, ссылки и локальные рисунки согласованы с новыми границами. `check-source` проходит для всех 14 лекций; добавлена проверка продолжений импортированных frames без ослабления собственного покрытия. `finalize 6` (Node 24.19.0) прошёл: **19 тестов**, web build и оба PDF. Сборки и экспорты выполнены последовательно в изолированной копии с физически отдельными зависимостями; работающие серверы исходного проекта сохранены.

Просмотрены все **44 страниц раздатки и 101 страниц PDF с раскрытиями**, плотные выводы и новые блоки дополнительно при ширине 1280 px. Все страницы раздатки попиксельно совпадают с соответствующими финальными состояниями. Браузерный инспектор прошёл все **44 слайдов / 101 состояний**: геометрия стабильна, клики содержательны, обратные шаги воспроизводятся, формулы и изображения загружены, переполнений и HTTP/JS ошибок нет. Стиль сравнен с утверждённой L1: в PDF L7 33/44 и L1 35, в браузере L4 33 и L1 35; Arial/KaTeX, bold vectors/parameters, KL/expectations, заголовки и цвета согласованы. Общая тема и макросы в этой задаче не менялись.

Артефакты QA: `../output/qa/through-lecture7-2026-09-26/`. Физическое перо и проектор повторно не проверялись. Исторические результаты ниже относятся к прежним редакциям.

SHA-256 текущей редакции: slides.md `e802074af6576274ad0b284d3b94542f93154d89c2c3e60e10e2f34dfc93fb65`; slide-map.json `b7970be2b7ec4743ec8acba9d421cdac7f365a683477171da9a99be97fb11b2c`; Lecture6.pdf `cbf3c6fb0b0c1d97a44ffb78f2d43b3e70464df2c65d19ac5994beb11df9dada`; Lecture6-handout.pdf `99032ca8b293980516e4cfaf3dddb525f864c08e8f1b6a9a997459febc6afe29`.

## Исходная редакция

- Beamer: [Lecture6.tex](../../lectures/lecture6/Lecture6.tex) и [Lecture6.pdf](../../lectures/lecture6/Lecture6.pdf), 78 страниц с раскрытиями.
- HEAD при переносе: `19e673be350344c8ea4fafffb78fa389d8b65e19`; последний коммит исходника L6 — `6ffa2cd6c9fe424f4abee28c412080289c0f4027`.
- SHA-256 TeX: `09f04a96a114411aef524c5941e9381eb54754d106542fe64b60dd2d52270af2`.
- SHA-256 исходного PDF: `696a11ac1404d0b5aaac8d9331f170f4d0c8aeb84ed487d91c958f4d4ae62444`.

## Покрытие и отличия

- [slides.md](slides.md) и [slide-map.json](slide-map.json): 35 логических слайдов, все 31 исходный frame, четыре автоматических Outline, 43 клика и 78 состояний. Порядок сохранён; слияний, продолжений и новых учебных примеров нет.
- Намеренный повтор DSM (frames 21 и 26, слайды 24 и 29) сохранён. В первом Langevin Dynamics появляется вместе с последним вариантом objective, во втором — отдельным кликом, как в Beamer.
- Сохранены десять Recap frames, определения, предпосылки, все шаги выводов, смысловые цвета и ссылки, полные Training и Sampling (Annealed Langevin Dynamics), четыре статических пункта Summary. Заметок преподавателя в исходнике нет.
- Все 15 локальных PNG/JPG побайтно совпадают с оригиналами; сохранены все 15 использований исходных картинок. Общая taxonomy использует `score-matching`, соответствующий исходному `sm`, и эталонный размер `class="taxonomy"`.
- Общая тема, шрифты, размеры и headmatter — из Lecture 1. `favicon: "data:,"` следует существующей практике L3/L4 и исключает внешнюю загрузку иконки. TeX `violet` адаптирован к утверждённому `#8854c0`; имена `teal` и `olive` сохранены как в предыдущих Slidev-лекциях.
- Формулы остаются редактируемыми. Длинные выражения разложены на строки `aligned`, пошаговые выводы используют накопительные диапазоны строк и явные `v-click`. Произвольного уменьшения шрифта и масштабирования нет.
- В Recap Wasserstein (слайд 5) уменьшена высота исходного изображения; в Recap precision/recall (слайд 9) парные формулы размещены в одной строке. Это устранило пересечение с источниками без сокращения содержания.
- Все раскрытия накопительные: 35 финальных состояний раздатки сохраняют весь материал. Beamer, соседние источники и `lectures/merged/` не менялись. Незакоммиченная работа других задач сохранена.

## Проверки исходников и связей

- `check-source.mjs 6` прошёл на окончательной редакции: 31 frame, четыре перехода, 35 слайдов, 78 состояний; карта, 22 прямые библиографические ссылки и 16 использований изображений с taxonomy проверены. Все 87 макросов адаптера актуальны.
- Дополнительно проверены ссылки из составных Beamer-footnotes и полнота математических выражений по frames. При переносе не утрачены изображения, обозначения, параметры, условия или шаги доказательств. Повторные знаки равенства на границах строк Beamer заменены эквивалентным выравниванием.
- Входящий Recap: окончательная L5 Slidev → L6 Slidev. GAN optimality, Expectations/Reality, GAN objective и mode collapse, disjoint supports, Wasserstein/dual/WGAN, FID, precision/recall, CLIP и human evaluation согласованы. Исходные сокращения Recap сохранены; интерактивное KL-продолжение L5 не изменяет эти определения.
- Исходящий Recap: L6 Slidev → окончательная L7 Slidev. Langevin, Fisher divergence, Gaussian kernel/assumption/theorem DSM, noise-level tradeoff, NCSN objective, Training и Sampling согласованы. Двухиндексная запись annealed Langevin сохранена.
- Summary сохраняет четыре исходных тезиса и покрывает оба раздела с обоими подразделами. Разделы и порядок совпадают с Materials корневого README. В каталоге Slidev добавлены исходник, оба PDF и журнал.

## Сборка и визуальная проверка PDF

- Использована изолированная копия `/private/tmp/dgm-l6-isolated/lectures-slidev` с физически отдельным `node_modules` (`cp -cR`), Node `24.19.0`, Slidev `52.19.1` и существующим lock-файлом. Dev/build/export в этой копии выполнялись последовательно.
- Окончательный `finalize 6` прошёл: проверка источника, все 13 существующих тестов, production build и оба экспорта.
- [Lecture6.pdf](Lecture6.pdf): 78 страниц с раскрытиями; [Lecture6-handout.pdf](Lecture6-handout.pdf): 35 страниц раздатки. `render-qa.py 6` прошёл.
- Просмотрены все 113 страниц обоих PDF. В масштабе 1280 × 720 дополнительно проверены страницы раздатки 5, 7–9, 20, 22–29, 33–34 и промежуточные страницы 40–41, 45–46, 49–50, 53–54, 59–60 полного PDF. Скрытые части не проявляются; обрезки, наложений, потери промежуточных выводов и пометок занятия нет.
- После исправления размера taxonomy повторены finalize и render-qa. 111 остальных страниц попиксельно совпали с уже просмотренной редакцией; две изменённые страницы (полный PDF 21, раздатка 17) просмотрены заново в полном размере.
- PDF-сравнение при одинаковой ширине 1280 px: L6 слайд 2 ↔ L5 слайд 13 (GAN/JSD, ожидания и векторы); L6 слайд 5 ↔ L5 слайд 22 (Wasserstein, интегралы, индексы); L6 слайд 7 ↔ L5 слайд 27 (дуальность и ограничения); KL/ожидания/векторы дополнительно сверены с L1 слайдом 35. Типографика, оформление блоков и смысловые цвета соответствуют эталону.

## Браузер и общая инфраструктура

- После окончательного экспорта dev запущен заново; полный строгий проход инспектора с дополнительными сценариями проверил все 35 слайдов и 78 состояний. Пустых кликов, сдвига геометрии, ошибок KaTeX, сырых delimiters, отсутствующих ресурсов и пересечений границ/источников нет.
- Для каждого слайда проверены обратные клики и возвраты с обоих соседних слайдов. Состояния воспроизводятся. Pen, Undo/Redo, Save/download, Save & clear, Restore и координаты пометок после раскрытий/возврата проверены синтетическим вводом; тестовые пометки очищены.
- При блокировке внешней сети все ресурсы L6 доступны: ноль внешних запросов, ошибок страниц и HTTP failures. Серверы временной копии остановлены после проверок.
- В браузере при 1280 × 720 повторены сопоставления L6 2/5/7 с L5 13/22/27 и L1 35. Шрифты, математические индексы, интегралы и повторённая дуальность согласованы с PDF и общей темой. Сравнение исходящего Recap в PDF/браузере также выполнено агентом L7.
- Координатор добавил в общий компонент и генератор только новые варианты выделения taxonomy (`gan`, `sm`, `ddpm`); существующие SVG, CSS, макросы и зависимости не изменены. В отдельной копии пересобраны оба PDF L1–L4, всего 469 страниц: L1/L2/L4 попиксельно совпали; L3 совпала по всему тексту и пикселям, кроме случайной формы существующих подчёркиваний `v-mark`. Все различия ограничены непосредственно штрихами. Рабочие PDF L1–L4 не перезаписывались.
- Контроль существующих вариантов taxonomy и математических примеров в браузере выполнен на L1 11/35/38, L2 8, L3 22, L4 8/15; общая панель пера также прошла проверку на L1. На старом L1 36 инспектор отметил выход bbox индекса θ на 0.45 px за порог 672 px; его PDF попиксельно совпал с исходным, L1 не редактировалась.
- QA-артефакты: `/private/tmp/dgm-l6-isolated/lectures-slidev/output/qa/lecture6/`. Полный отчёт `browser-report.json` включает reentry и annotation integration; `final-pdf-comparison.json` фиксирует две изменённые страницы. Дополнительный инспектор находится только во временной копии и расширяет текущий `inspect.mjs`.

## Финальные SHA-256

- Markdown: `36a78404d217ab19924fbe892800c98ad727ee61b76a1a1f84c705826a462b72`. Перед коммитом 2026-09-16 удалены пять конечных пробелов после пустых `class:`; содержимое и YAML-значения не изменились. Проверенная редакция до этой чистки: `91c07db8a07fc7548cc60a4ce75ab881ead4ade5df13055075435abfae878ceb`.
- PDF с раскрытиями: `934ec643ee64db68f4434dca3773588f1be41a367e098eaa18b66605f45e632c`.
- Раздатка: `10f35d920da7dc32e51245ee269ca12bd3382789e91650ce5317448ca3f3c231`.

## Сохранённые формулировки и ограничения

Редактура математических утверждений не выполнялась. Сохранены исходные записи `const(θ)` в теореме DSM, градиент по `x` в noisy objective, приближённое совпадение score при малом шуме и неформальная стационарность Langevin при малом шаге. Повтор DSM соответствует решению автора и не является дефектом переноса.

Физический стилус, palm rejection, проектор и показ с отдельного планшета не проверялись. Проверки мышью и в headless-браузере не подтверждают поведение этих устройств.
