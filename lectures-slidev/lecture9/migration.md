# Lecture 9: migration to Slidev

Completed on **2026-09-16** using the course [migration workflow](../MIGRATION.md).
The Beamer content, eight Recap frames, section order, source credits, equations,
semantic colors, original algorithms, and static five-bullet Summary are preserved.
No lecture-specific fonts, scaling, macro definitions, or theme overrides were added.

## Baseline and artifacts

- Repository baseline: `19e673be350344c8ea4fafffb78fa389d8b65e19`.
- Beamer source: `lectures/lecture9/Lecture9.tex`, SHA-256
  `5ff5ede775ed5b1f39988a369032b262b88e4ab6e3551945d30efb0f1eec7069`.
- Beamer PDF: **74 pages**, SHA-256
  `0319832fa7d26d55dc70ebef70ee538eb8b9bd884f8471dd763d6c252a2c4ed7`.
  Its adjacent `.fdb_latexmk` source MD5 matches the current `.tex`.
- Shared notation source SHA-256:
  `85df378eb646f74b973e84a3c38add1ac827ebccb943b3b1e4ae347b659f809a`.
- [slides.md](slides.md): SHA-256
  `b81b8b68952d019a102693537fdf4e153da6bbc08c430a3e239dba0fb23f3d36`.
- [slide-map.json](slide-map.json): SHA-256
  `1e92b02963a1ea29f19592a099051898e7d0cccc8acedeeafed8ad135cb386ce`.
- [Lecture9.pdf](Lecture9.pdf): **77 pages**, SHA-256
  `00c2a151059b2aacda7a2017e01924cf11e92411e4043f81786e0ccade2d5aa2`.
- [Lecture9-handout.pdf](Lecture9-handout.pdf): **38 pages**, SHA-256
  `8658a0f298683db3aac15ff8cd21fe742e18c1eb1274d4bd3ac9b993f09b0324`.

Beamer files and `lectures/merged/` were not changed. The parent migration task owns
the shared taxonomy additions and the Slidev README catalog entry.

## Frame coverage and reveals

All **30 explicit source frames** and **3 automatic section Outline transitions**
are covered. The result has **38 logical slides**, **39 within-slide clicks**, and
**77 rendered states**. Every slide has explicit `sourceFrame` and `clicks`, with
matching `slide-map.json` records; no source frame is merged, omitted, or duplicated.
Outline slides contain only the original three-section agenda.

The source has 41 reveal directives. Two become continuation-slide boundaries;
three static Recap splits explain the three extra rendered pages relative to Beamer.

| Source frame | Slidev slides | Preserved progression / reason for split |
|---|---|---|
| 3 | 3–4 | Mean reparametrization, then noise prediction and the simplified objective; static source. |
| 8 | 9–10 | Guided-score scale and sharpening, then complete Training/Sampling blocks; static source. |
| 9 | 11–12 | All three CFG equalities, then the scaled score and CFG procedure; static source. |
| 14 | 18–19 | ODE and flow, then numerical solution, approximation fragment, and solver explanation. The original pause before the numerical-solution block becomes a slide boundary. |
| 15 | 20–21 | Numerical routine and Euler update, then the illustration and Heun update. The source's final reveal of the image and Heun block becomes a slide boundary. |

All equation rows remain editable KaTeX. The mean row's inherited unmatched-looking
TeX grouping is normalized as in Lecture 8 without changing its complete expression.
Long CoV and ELBO expressions use aligned rows. Within-line solver approximations use
the approved shared `math-chain` layout with explicit relation spacing. The ODE
integral appears through a cumulative aligned-row reveal. No fraction, root,
underbrace, or paired delimiter is split into independently revealed fragments.

Source PDF pages **19–30** were viewed to verify the nontrivial ODE/integral/solver
reveal order. In particular, the iterative-routine explanation appears with the
solver approximation, while the final source Heun image and equations stay together
on their continuation. Recap Training/Sampling remains static where the source is
static; whole steps and original titles are retained. There are no source `\note`
commands, replacement states lost from the handout, or new interactive demos.

## Sources and assets

The source check validates all 16 direct linked citation occurrences and five image
uses. The nested `\href` credits in source frames 6 and 9 were checked separately;
all URLs and attribution text are retained. Continuations repeat their source credits.
The four original bitmap assets are copied byte-for-byte:

| File | SHA-256 |
|---|---|
| `brownian_motion.png` | `e95e68e626dddffcde9e571a76f48a30f04d3338d66ae699f0ad07c5c94a588e` |
| `cnf_flow.png` | `2485f9b7208f4834e0fd62f1bf6af7cf72fb606fd94887e5e20901bd022f0137` |
| `heun_method.jpg` | `4ad2eaab1ea42afe9942f4a0e78269e76667a9497b19b3e9fea9a1b321203b61` |
| `normalizing-flow.png` | `506f2ea2e540fca6421f3601b41eeb38d205f2df153e5099c402e80618ea7f37` |

The taxonomy uses `<TaxonomyDiagram class="taxonomy" continuous-normalizing-flow />`
and the shared `taxonomy-ode.svg` generated by the parent task from the unchanged
course TikZ. The parent separately checked the shared additions against Lectures 1–8: 1001 exported pages matched their approved renders, apart from 15 existing randomized `v-mark` pages in Lecture 3 whose unmarked regions remained identical. Prior main-workspace PDFs were not replaced.
Violet uses the approved `#8854c0` palette; teal, olive and gray retain their meanings.
Headmatter follows Lecture 1; the local data-URL favicon avoids a remote dependency.

## Source audits and interfaces

The migration was checked using `lecture-audit`, `notation-lint`, `recap-sync`,
`summary-sync`, and `readme-sync` in Slidev mode.

- **Notation/content:** all 87 shared macros match; sourceFrame coverage, derivation
  steps, local assets, sources, semantic colors, and algorithm grouping pass.
  Additionally, all **194 math expressions** (**63 display, 131 inline**) were parsed
  directly with KaTeX `strict: 'error'` and `throwOnError: true`, using shared macros.
  This supplements the browser inspector's narrower raw-math/error checks.
- **Incoming Recap:** Lecture 8 Slidev → Lecture 9 Slidev matches the Gaussian ELBO,
  variance assumption, mean/noise parametrization, DDPM algorithms, DDPM/NCSN
  objectives/summary, classifier guidance, and CFG. The original concise scope and
  source editorial choices are retained. Lecture 8 source SHA-256:
  `ee1f2946edb310a6ea18938d879aee24f8683670c7484cab29573401a323b960`.
- **Outgoing Recap:** Lecture 9 Slidev → concurrently migrated Lecture 10 Slidev
  recap frames 2–7 matches the ODE integral/Euler step, probability path, Picard
  reversibility, continuity theorem and solution, SDE/Wiener/Euler-Maruyama, and
  KFP/Langevin formulas. The source `cnf_flow.png` is identical. The inherited
  condensed wording and Runge-Kutta remark remain. Compared Lecture 10 source:
  `c9a725dc8bf19f71cbb0b094d7ddbf2fe341b46ddbeeeff60dacbb2647af2d94`.
  Findings were shared with its migration worker; no correction was required.
- **Summary:** the five original static bullets cover CNFs/neural ODEs, probability
  paths and the continuity equation, SDE drift/diffusion, KFP, and stationary Langevin.
- **Schedule:** the root Materials row for Lecture 9 matches the three sections and
  their order exactly; no root schedule update is required. The parent owns the new
  Slidev artifact-catalog links.

### Preserved source editorial issues

These are inherited, not new migration defects, and were not silently rewritten:

- The recap NCSN objective omits the earlier `\sigma_t^2` weighting, as recorded in
  Lecture 8. The ELBO still carries `\bphi` for its fixed forward process; generalized
  `\alpha_t` in the DDPM/NCSN summary retains the source convention.
- Frame 14 uses `\bpsi_0(\bx_0)=\bx_0` while its surrounding interval starts at
  general `t_0`. Frame 21 writes a Wiener increment as equal to a Normal distribution,
  rather than using a distributional symbol; the formal infinitesimal shorthand remains.
- Frame 27's KFP necessity/sufficiency and uniqueness statement omits conditions.
  Frame 28 passes from stationary divergence to zero probability current, selecting
  the usual sufficient Langevin construction without explicitly stating that choice.
- "Constant density" means constant in time in the source's explanatory parenthesis.
  The source wording and the original `Langevin Dynamic` title remain.
- Existing Training blocks without an optimizer update retain their original scope.

## Rendering, browser and style evidence

All dev/build/export commands ran sequentially in a physically separate installation
at `/private/tmp/dgm-slidev-l9-20260916/lectures-slidev` with a cloned, nonsymlinked
`node_modules`. They did not use or overwrite the shared project's generated client.

1. `node tools/run.mjs finalize 9` passed source checks, the existing numerical/course
   tests (**13 passed, 0 failed**), static build, and both PDF exports. `tools/render-qa.py 9` passed counts and
   text-delimiter checks and rendered every page.
2. All **38 handout pages** and **77 reveal pages** were visually reviewed on contact
   sheets. Dense handout pages **2, 3, 4, 5, 11, 15, 17, 20, 21, 25, 27, 29, 31, 34,
   35, 36, 37** were also inspected at 1280×720. Formulas, plots, and multi-line source
   footers are clear, unclipped, and separated. Future material is hidden; the handout
   retains every derivation row and algorithm.
3. After the final two-slide solver-spacing polish, both exports were rebuilt and
   rendered. Pixel comparison against the reviewed version found **109 of 115 pages
   identical**. The six changed pages (handout 19–20; reveal 28–29, 31–32) were separately
   reviewed at full size. No other content or geometry changed.
4. `inspect.mjs 9` passed all **38 slides / 77 states** after export: unchanged geometry,
   nonempty clicks, backward-state identity, no content/source boundary overflow,
   loaded images, no page/HTTP errors, and no raw math/error nodes. The final spacing
   change was followed by another successful inspection of slides 19–20.
5. Additional browser checks traversed all **37 nonfinal slide returns / 76 states**
   and matched every backward state after reentry. External HTTP(S) was blocked;
   no external resource request or page error occurred. The final Summary is static
   and was covered by the full inspector. No network data or external font is needed.
6. On slide 36, a synthetic annotation passed drawing, Undo/Redo, Save, Save & clear,
   Restore, forward steps, departure/return, and an identical saved SVG after return.
   Test archives stayed in temporary QA output; the synthetic mark was cleared.

Actual style comparisons used **1280×720** PDF renders and browser captures:

- Lecture 1 slide 27 (LOTUS/CoV) ↔ Lecture 9 slides 2 and 15: vector and expectation
  glyphs, determinant/Jacobian, headings, blocks, and common layout.
- Lecture 1 slide 35 (KL/MLE) ↔ Lecture 9 slide 2: `\KL`, `\pt`, bold parameters,
  delimiters and semantic hierarchy.
- Lecture 8 slide 20 ↔ Lecture 9 slide 3: identical Gaussian-loss and mean
  reparametrization formulas, including indices, colored coefficients, and roots.
- Lecture 8 slide 22 ↔ Lecture 9 slide 2: the full Gaussian ELBO and underbrace.
- Lecture 8 slide 46 ↔ Lecture 9 slide 10: Training/Sampling structure and guided score
  were compared in the browser; the first two Lecture 8 pairs were also inspected
  in PDF at full scale.

The reference captures were generated by the parent coordinator from the current
approved sources and inspected alongside this lecture. CSS metrics agree: Arial
37px/650 titles, 24px/650 block titles, 24.96px KaTeX, 12px sources, and shared colors.
The shared `math-chain` style is the same approved 23px layout used by Lecture 1.
No font substitution or lecture-specific size adjustment was used.

Temporary evidence is in `output/qa/lecture9/` of the isolated installation and
`/tmp/dgm-slidev-parent-20260916/reference-captures/`; it includes contacts, full-size
PDF pages, screenshots, browser reports, strict KaTeX results, source hashes, and
final PDF pixel comparison. No temporary files are required to present the lecture.

**Device limits:** a physical stylus, palm rejection, tablet/projector connection,
and presentation hardware were not available. The synthetic browser annotation
check does not establish those device behaviors. Presenter/viewer widget-state
synchronization is not claimed; no new widget was introduced.

## Final course integration — 2026-09-17

The final catalog integration is complete: the Slidev README links the editable source, both reviewed PDFs and this journal for all Lectures 1–14; the root README now points to the full catalog. The existing Materials hierarchy and schedule comment wrapper were preserved. Final main-workspace checks passed for the source/map/assets, all 194 strict KaTeX expressions, PDF page counts and the artifact hashes recorded above. This documentation-only integration did not change the reviewed lecture source or PDF bytes.
