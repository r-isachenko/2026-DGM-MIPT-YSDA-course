# Lecture 3: Slidev

## Удаление демо с фиксированным шумом, 2026-09-15

По запросу автора удалён отдельный слайд «Reparametrization: Freeze the Noise» (прежний слайд 25, `extension: 19`): автор считает его недостаточно содержательным. Текущая версия — **26 логических слайдов и 74 состояния**. После исходного вывода репараметризации на слайде 24 сразу следует исходный расчёт градиента ELBO на слайде 25; Summary теперь на слайде 26.

- Из Markdown удалён только этот дополнительный слайд с тремя состояниями. Остальной текст, формулы, раскрытия, Recap и Summary побайтно сохранены. Все 21 исходный frame и четыре Outline остаются; единственное дополнительное `extension` — схема амортизации.
- Удалены компонент `ReparamDemo.vue`, его состояние, генератор шума, функция репараметризации и относящиеся к ним численный тест и браузерные сценарии. ELBO и его проверки сохранены. README и карта обновлены.
- `finalize 3` прошёл: проверка исходников, **13 тестов**, production build и оба PDF. `render-qa.py 3` подтвердил 74 страницы с раскрытиями и 26 страниц раздатки. Визуально просмотрены все 100 страниц; страницы раздатки 24–26 дополнительно проверены крупно. Удалённого демо и нарушений вёрстки нет.
- `inspect.mjs 3` проверил все 26 слайдов и 74 состояния: ошибок, выхода за границы и нестабильных обратных раскрытий нет. Демо ELBO прошло шесть визуальных состояний, пресеты, крайние значения, клавиатуру, возвраты, Reset, offline и synthetic pen. После первого запуска с незавершённой загрузкой стилей dev-сервера сценарий повторён последовательно и завершился успешно.
- Использована изолированная рабочая копия. Общая тема, зависимости, исходные Beamer и другие лекции не менялись. Ограничения реального устройства и presenter/viewer-синхронизации из предыдущего журнала остаются применимыми.

Итоговые SHA-256:

- Markdown: `1b0becbab57951ee7a84785eda795a7f95cfc008a2f2e306a81679327ae0a936`.
- PDF с раскрытиями: `d8dddeb1521b519ab3c1bf3e9449a4391a937902bfee1b2b7962683f8b13a239`.
- Раздатка: `e5772c647c9606cdf189df7d21be1276e6e9f5e480bff9310117f442b28f0227`.

Текущие артефакты проверок: `output/qa/lecture3/remove-reparam-2026-09-15/`. Следующий раздел — историческая запись версии с четырьмя дополнениями до удаления этого демо.

## Интерактивные дополнения до удаления демо, 2026-09-15

Базовый перенос сохранён отдельным коммитом `bb14aef7` (`Migrate Lecture 3 to Slidev`). По запросу автора реализованы четыре согласованных дополнения. Текущая версия содержит **27 логических слайдов и 77 состояний**; базовые 25 слайдов и 70 состояний сохранены, добавлены два поясняющих слайда. Общий регламент — [MIGRATION.md](../MIGRATION.md), запуск и управление — [README.md](../README.md#интерактивная-lecture-3).

### Что изменилось

- Слайд 14, исходный frame 12: `ElboDemo` заменяет две растровые иллюстрации Gaussian posterior. Показаны фиксированное evidence, текущие ELBO и KL gap, плотности точного и вариационного posterior. Poor / Better / Exact, ползунки среднего и стандартного отклонения и Reset позволяют проверить, как приближение становится точнее. Формулы и исходные пять раскрытий сохранены; исходные PNG остаются в `public/figs/`.
- Слайд 18, `extension: 14`: нативная схема `Amortized Inference: One Shared Encoder`. Три раскрытия `v-click` сопоставляют отдельную оптимизацию для каждого наблюдения с общей сетью, выдающей параметры условного вариационного распределения. Исходный слайд 17 сохранён. Отдельный слайд позволяет оставить стандартный размер текста.
- Слайд 25, `extension: 19`: `ReparamDemo` с фиксированными Gaussian noise samples и преобразованием `z = μ + σε`. При изменении μ / σ шум не меняется; Identity / Shift / Stretch дают три примера, Resample noise создаёт новую выборку, Reset возвращает исходную. Соответствующие точки имеют одинаковые цвета и общую шкалу. Два раскрытия показывают детерминированный путь градиента reconstruction term и производные по μ / σ. Исходный вывод на слайде 24 сохранён.
- Слайды 22–24 и 26, исходные frames 17–20: смысловые цвета и штатные `v-mark` подчёркивают зависимость от θ / φ и место дифференцирования. Все исходные формулы и числа раскрытий сохранены. В примечании явно указано, что распределение шума `p(ε)` не зависит от φ.

Схемы и графики вычисляются локально; новых внешних изображений или сетевых зависимостей нет. Дополнительные иллюстрации реализованы в исходниках лекции; ссылки на теоретические результаты сохранены. Общие шрифты, размеры, тема, макросы, зависимости и lock-файл не менялись. Новые заметки преподавателя и quiz не добавлялись. Beamer, `lectures/merged/` и другие лекции этим дополнением не редактировались.

### Модель и управление

- ELBO рассчитан для нормированной модели `z ~ N(0, 1)`, `x | z ~ N(z, 1)` при наблюдении `x = 2`. Точный posterior — `N(1, 1/2)`, где второй параметр — дисперсия. ELBO вычисляется через ожидаемый log-likelihood минус KL к prior; тождество с KL к posterior проверяется отдельно. Изменение вариационного распределения не меняет evidence.
- Reparametrization использует воспроизводимый генератор Box–Muller с исходным seed `53`. Только Resample noise меняет выборку. Reset восстанавливает seed `53`, μ = 0, σ = 1. Контрольные производные: `∂z/∂μ = 1`, `∂z/∂σ = ε`.
- Общая существующая панель `DemoPanel` изолирует события управления от переходов Slidev и слоя пера. Состояние компонентов в памяти сохраняется при уходе на соседний слайд и возврате. В PDF для каждого демо одновременно показаны три подготовленных примера; раскрытия пути градиента остаются пошаговыми.
- Управлять демо нужно в выводимом на экран окне. Произвольное состояние этих компонентов между presenter/viewer не синхронизируется. После полной перезагрузки страницы значения возвращаются к исходным.

### Проверки

- Полная независимая проверка нотации и математических выводов, входящего Recap, исходящего материала для Recap L4 и Summary: введённых ошибок или пропусков не найдено. Исходные 21 frame и четыре Outline сохранены; в карте добавлены только две явные `extension`. Входящий Recap и Summary совпадают с зафиксированной базой. Дополнения иллюстрируют уже изложенную теорию и не требуют правки L4.
- `finalize 3` прошёл: проверка исходников (27 слайдов, 77 состояний, 87 макросов, 7 распознанных ссылок и 3 использования изображений), **14 тестов**, production build и оба PDF. Три новых теста проверяют ELBO независимым численным интегрированием, неотрицательность KL и точный оптимум, воспроизводимость шума, моменты Gaussian samples, производные конечными разностями и Monte Carlo gradient.
- [Lecture3.pdf](Lecture3.pdf): **77 страниц с раскрытиями**. [Lecture3-handout.pdf](Lecture3-handout.pdf): **27 страниц раздатки**. `render-qa.py 3` прошёл. Визуально просмотрены все 104 страницы обоих итоговых PDF, а изменённые страницы раздатки 14, 18, 22–26 дополнительно проверены при ширине 1280 px. Обрезок, наложений, утечки будущих раскрытий и пометок занятия нет.
- После финального экспорта `inspect.mjs 3` повторно проверил все 27 слайдов и 77 состояний: ошибки браузера и KaTeX, недоступные ресурсы, пустые клики и выходы за границы отсутствуют; геометрия и обратные раскрытия стабильны.
- `inspect-variational-demos.mjs` проверил 12 визуальных состояний обоих демо, пресеты, крайние значения ползунков, численные показания и неподвижное evidence, сохранение фиксированного шума, Resample / Reset, управление с клавиатуры без перехода, возврат с обоих соседних слайдов, mouse / synthetic pen при включённом пере. При блокировке внешней сети ошибок и внешних запросов нет. Все 12 состояний просмотрены визуально; заключительный прогон повторён для итогового Markdown.
- Проверки выполнялись с Node `24.19.0`, Slidev `52.19.1` и отдельным `node_modules` в изолированной рабочей копии. Сборка, экспорт и dev в этой копии выполнялись последовательно. Работающие серверы других лекций не останавливались. Новые численные тесты добавлены в `test:demos` и `finalize`; код экспорта не изменён.
- Общие файлы темы, используемые L3, совпадают с проверенной версией. Параллельное дополнение `TaxonomyDiagram` для L4 сохранено; L3 этот компонент не использует. Историческое сравнение с утверждённой Lecture 1 остаётся применимо к неизменённой общей теме.

Физический стилус, palm rejection, проектор и отдельный планшет не проверялись; synthetic pen подтверждает только обработку браузерных событий. Унаследованные редакторские оговорки сохранены в журнале базового переноса ниже.

Итоговые SHA-256:

- Markdown: `10a973f9329396b4a5057e79981be2163b4c5cb2b2e7a53343dc665400b7348d`.
- PDF с раскрытиями: `558d38e799a443475b4fffc046d5e807cef048fb9338e52f4813d45fb50ad007`.
- Раздатка: `ff75f9a99d2f6fbed0e980d39699d8f22f4c002ce1d28480391e455371e9af2c`.

Артефакты проверок сохранены вне Git в `output/qa/lecture3/interactivity-2026-09-15/`: итоговые отчёты браузера, журнал finalize, контактные листы и крупные страницы PDF, сценарии демо и manifest файлов.

## Базовый перенос, коммит bb14aef7

Ниже сохранён исторический журнал редакции от 2026-09-14. Указанные в нём 25 слайдов, 70 состояний и хеши относятся к базовому коммиту; текущая редакция описана выше.

Перенос «как есть» выполнен 2026-09-14. Общий регламент — [MIGRATION.md](../MIGRATION.md), команды — [README.md](../README.md).

### Исходная редакция

- Beamer: [Lecture3.tex](../../lectures/lecture3/Lecture3.tex) и [Lecture3.pdf](../../lectures/lecture3/Lecture3.pdf), 70 страниц с раскрытиями.
- HEAD репозитория на момент переноса: `b44cc7eb7ce6bb3ee1f421e159b7ae39e620c0f2`; последний коммит исходника L3 — `28a420d2`.
- SHA-256 TeX: `36fd6d3056e7031329b5b50a418424d25aadc92a619f7da3134f865221d1594e`.
- SHA-256 исходного PDF: `06dde5a96c1d3c15df4c917e537d8a33e84469fcffa971c5fac5cd0842724a67`.

### Покрытие и отличия

- [slides.md](slides.md) содержит 25 логических слайдов: все 21 исходный frame и четыре автоматических Outline. Соответствие и число кликов — в [slide-map.json](slide-map.json). Слияний, продолжений и новых учебных примеров нет.
- Сохранены все 70 содержательных состояний. Нумерация страниц PDF с раскрытиями совпадает с исходным Beamer PDF; каждое состояние сверено отдельно, включая раскрытия внутри формул. Финальные состояния кумулятивны, поэтому 25 страниц раздатки сохраняют весь материал.
- Текст и математика редактируются в Markdown/KaTeX. Используются существующие общие тема, параметры headmatter Lecture 1 и адаптер 87 макросов. Общие стили, зависимости, экспорт и компоненты не менялись.
- Все пять PNG в `public/figs/` побайтно совпадают с оригиналами: `flows_how2.png`, `lvm_diagram.png`, `em_bishop1.png`, `em_bishop2.png`, `em_bishop4.png`.
- Сохранены все источники, повторения, вопросы, смысловые цвета и три исходных тезиса Summary. Outline содержит только план. Training/Sampling в исходной L3 отсутствуют; дополнительные блоки не вводились.
- В recap Gaussian AR NF применена утверждённая терминология: `density estimation can be parallelized` → `density evaluation can be parallelized`.
- Во время переноса автор согласовал сжатие NF Examples в L2 (раздел «Compress NF examples (2026-09-14)» её журнала). Согласно явному указанию для интерфейса L2 → L3, в frame 4 блок LU/QR заменён текущим `Structured Factors`, а `\btheta=\bW` удалено: параметры теперь задаются факторами. Общие AR identities сохранены без переноса частного условия `σ = 1`. Это единственная содержательная адаптация исходной L3.
- Длинные математические цепочки сверстаны через `aligned` и фрагменты `v-click`, без изменения выводов. Пурпурный соответствует общей теме `#8854c0`. Размеры шрифтов не уменьшались.
- В headmatter L3 задан `favicon: "data:,"`: пустой встроенный значок вкладки заменяет стандартный запрос Slidev к CDN. На изображение слайдов это не влияет.
- Новых интерактивных демонстраций нет. Используется существующая панель пометок общей темы.
- Beamer, предыдущие лекции и `lectures/merged/` не изменялись. Текущие незакоммиченные правки L2 сохранены.

### Проверки исходников

- `check 3`: 21 исходный frame, четыре перехода, 25 слайдов, 70 состояний; карта и headmatter согласованы, ссылки и изображения доступны, 87 макросов актуальны.
- Полная независимая проверка нотации, индексов, пределов, знаков и шагов вывода: введённых ошибок и пропусков не найдено. Сверены все предложения и все 70 исходных страниц.
- Входящий recap: L2 Slidev → L3 Slidev. CoV, NF log-likelihood, текущий Structured Factors, AR, coupling, MLE и LVM согласованы. Новые демонстрации и дополнительные темы L2 в краткий recap не добавлялись.
- Согласованная редакция L2 при финальной сверке: SHA-256 `c7985b04ff1fa7e77aae42f540681460f65278915e43a5e99294a1eeb59c1758`. Формула линейного потока и текст Structured Factors совпадают с этой редакцией.
- Исходящий recap: L3 Slidev → L4 Beamer. ELBO, амортизация, gradient update и Gaussian reparametrization согласованы. L4 не редактировалась.
- Summary дословно сохранён. Все четыре раздела совпадают с корневым README; L3 добавлена в каталог Slidev.

### Сборка и PDF

- Node `24.19.0`, Slidev `52.19.1`, существующий lock-файл. Использована изолированная рабочая копия с отдельным `node_modules`, чтобы не конкурировать с работающей L2.
- Итоговый `finalize 3` прошёл целиком: проверка исходников, все 11 существующих тестов, production build и оба PDF. Для временного локального порта экспортера использован разрешённый запуск вне песочницы. Версии и код экспорта не менялись.
- [Lecture3.pdf](Lecture3.pdf): 70 страниц с раскрытиями.
- [Lecture3-handout.pdf](Lecture3-handout.pdf): 25 страниц раздатки.
- `render-qa.py 3` прошёл. Просмотрены все страницы обоих PDF, а плотные страницы раздатки 2, 4, 10, 12, 14, 15, 21–24 проверены дополнительно при ширине 1280 px. Обрезки, наложений, видимого будущего материала и пропущенных состояний не обнаружено. Пометок занятия в PDF нет.
- После замены favicon все 95 страниц повторного экспорта попиксельно совпали с просмотренной версией. После обновления recap повторно просмотрена страница 4 обоих PDF; остальные 93 страницы попиксельно совпали с просмотренной версией.
- SHA-256 итогового Markdown: `0ce75a13cf88b3f753753e3797b53c0b67aeb31189ae065334bd50436ebe9aad`.
- SHA-256 PDF с раскрытиями: `dc0ed00454ea42a955a4dc78417ce44680e4e868fd3b5317cf7d87aa3f974c2a`.
- SHA-256 раздатки: `02573ff05cc871af8e4aac9e69d4bba9203aa10f84abef2c1d5a456d602e1679`.

### Браузер и соответствие стилю

- После экспорта dev запущен заново. `inspect.mjs 3` прошёл для всех 25 слайдов и 70 состояний: нет ошибок KaTeX, сырых delimiters, недоступных ресурсов, пустых кликов, сдвига геометрии или выхода содержимого за границы и блок источников. Все обратные клики воспроизводят соответствующее состояние.
- Дополнительно проверены возвращения с обоих соседних слайдов для всех 25 страниц, pen/undo/redo/save/download/clear/restore и сохранение геометрии пометок при раскрытиях и возврате. Синтетические пометки очищены. При блокировке внешней сети все локальные ресурсы загружаются; внешних запросов и ошибок браузера нет.
- Сравнение при одинаковой ширине 1280 px в браузере и PDF: L3 слайд 2 ↔ L1 слайд 27 (CoV, `\bx`, `\bz`, `\bff`, `\bbR`, определители); L3 слайд 15 ↔ L1 слайд 36 (KL, ожидания, интегралы, накопительные строки). Дополнительно проверены PDF L2 слайда 9 и браузер L1 слайда 35. Шрифты, начертания, размеры, цвета и оформление блоков соответствуют общей теме; сетка адаптирована к исходному содержанию recap.

### Унаследованные редакторские вопросы и ограничения

Помимо явно согласованного обновления linear recap, прежние формулировки сохранены:

- AR использует условие `σ_j ≠ 0` без отдельной оговорки о гладкости функций параметров, как L2.
- Variational Posterior содержит сокращённую запись `\argmin_q\KL(q\|p)`, согласованную с L4.
- Первый тезис Summary формулирует максимизацию ELBO как получение MLE; при ограниченном семействе posterior это в общем случае приближение. Редактура тезиса не входила в перенос.
- В recap L4 отсутствуют некоторые источники повторённых результатов. Это прежний вопрос L4, а не дефект L3.

Физический стилус, palm rejection, проектор и показ с отдельного планшета не проверялись. Новых механизмов пера или presenter/viewer-синхронизации перенос не вводит.
