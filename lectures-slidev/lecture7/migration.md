# Lecture 7 — перенос в Slidev

## Перераспределение материала до лекции 7, 2026-09-26

По согласованному плану полный раздел `Forward Gaussian Diffusion Process` (исходные frames 9–14 и четыре продолжения) перенесён в конец Slidev L6. В L7 сохранены два статичных recap по собственным frames 11 и 13: переход и условное распределение с накопленным шумом, затем предел и мотивация обращения процесса. Основной разбор этих frames находится в L6. Прежний recap Langevin (frame 2) убран; score/DSM/NCSN recap frames 3–7 и их продолжения сохранены без изменений. В `omittedSourceFrames` перечислены 2/9/10/12/14, в `omittedSourceSections` — перенесённый forward-раздел.

В L7 импортированы L8 frames 9–17: завершение ELBO, репараметризация через предсказание шума и итоговый DDPM с Training/Sampling. Все четыре продолжения frames 10/12/13/14 сохранены как `extension: imported: 8:N`. Завершение ELBO непосредственно продолжает текущий раздел; отдельный переход `(continued)` больше не нужен. Два новых Outline относятся к `Gaussian Diffusion Reparametrization` и `Denoising Diffusion Probabilistic Model (DDPM)`. Все формулы, раскрытия, источники и порядок импортированного материала сохранены. Рисунок `diffusion_objective.png` скопирован из L8 побайтно. Summary пересмотрен целиком: шесть статичных тезисов про reverse process, условный Gaussian reverse, VAE, ELBO, noise prediction и DDPM.

Базовая редакция перед этой перестановкой: SHA-256 `slides.md` — `e13e9833bf1f316ae4d5342607806123be397efe5b226a4a1bb56fc2f7cecd7d`. Новая карта содержит **45 логических слайдов / 88 состояний**. Проверка исходников подтвердила побайтное сохранение 15 оставшихся body-слайдов L7 и всех 13 импортированных слайдов L8 с точностью до `sourceFrame`, а также совпадение двух новых статичных forward-recaps с телом L6. Карта, числа кликов и локальные пути рисунков согласованы. Beamer, общая тема, макросы и `lectures/merged/` не менялись.

Проверки текущей редакции завершены: README/Outline, входящий и исходящий Recap, Summary, карты, формулы, раскрытия, ссылки и локальные рисунки согласованы с новыми границами. `check-source` проходит для всех 14 лекций; добавлена проверка продолжений импортированных frames без ослабления собственного покрытия. `finalize 7` (Node 24.19.0) прошёл: **19 тестов**, web build и оба PDF. Сборки и экспорты выполнены последовательно в изолированной копии с физически отдельными зависимостями; работающие серверы исходного проекта сохранены.

Просмотрены все **45 страниц раздатки и 88 страниц PDF с раскрытиями**, плотные выводы и новые блоки дополнительно при ширине 1280 px. Все страницы раздатки попиксельно совпадают с соответствующими финальными состояниями. Браузерный инспектор прошёл все **45 слайдов / 88 состояний**: геометрия стабильна, клики содержательны, обратные шаги воспроизводятся, формулы и изображения загружены, переполнений и HTTP/JS ошибок нет. Стиль сравнен с утверждённой L1: в PDF L7 33/44 и L1 35, в браузере L4 33 и L1 35; Arial/KaTeX, bold vectors/parameters, KL/expectations, заголовки и цвета согласованы. Общая тема и макросы в этой задаче не менялись.

Артефакты QA: `../output/qa/through-lecture7-2026-09-26/`. Физическое перо и проектор повторно не проверялись. Исторические результаты ниже относятся к прежним редакциям.

SHA-256 текущей редакции: slides.md `a3b0b70473af1cc38592adb2abf5756f5c762ab3e079294ac320da68f9dadea5`; slide-map.json `f4c08b1bfa7187ec1ecd9dc8f7643cc4dea25e5ae07c7d19ae92c2ff785d0567`; Lecture7.pdf `ce007060d4c385dfcbfa50c20b0892cef2399d54768dde3f43fb95ed6f7e505e`; Lecture7-handout.pdf `ad169acc32047285795dd95f978ee7c28c6300d505fa74d24b6a187070e596fd`.

Перенос завершён 2026-09-15. Источник — текущая Beamer-лекция 7; её содержание, порядок, исходные повторы, Recap, Summary, формулы и раскрытия сохранены. Beamer-исходники и `lectures/merged/` не изменялись.

## Исходная редакция и артефакты

- Базовый коммит репозитория: `19e673be350344c8ea4fafffb78fa389d8b65e19`.
- `lectures/lecture7/Lecture7.tex`: SHA-256 `5f60a5ef58fd411d4392deda5ebd43a58a998403948cbe3a049389b45448bdc0`.
- Исходный `lectures/lecture7/Lecture7.pdf`: 80 страниц, SHA-256 `66339eb95909035c7babe661b543615db84128fce18999e829b2432b80f0286d`.
- [slides.md](slides.md): SHA-256 `e13e9833bf1f316ae4d5342607806123be397efe5b226a4a1bb56fc2f7cecd7d`.
- [slide-map.json](slide-map.json): SHA-256 `d9f666383e612b517bef8837e498527840a1433073b5940cab0d1bbf4ece5dd5`.
- [Lecture7.pdf](Lecture7.pdf): **82 страницы** с раскрытиями, SHA-256 `60cf13fa4c2cbb1114decc761222c911409955bdef144e6bbb824d8d6a17e13e`.
- [Lecture7-handout.pdf](Lecture7-handout.pdf): **40 страниц**, SHA-256 `e711415d77d7eccd5d4c0066ee206a11e9923f206d0a299ade8f6ab2f3e5f895`.

## Покрытие и компоновка

- 26 исходных frames учтены ровно по одному разу, добавлены четыре автоматических Outline. Порядок четырёх разделов и исходный общий Outline сохранены; Outline содержит только план.
- 40 логических слайдов, 42 внутренних клика. Восьми исходным паузам соответствуют переходы на продолжения. Два статических recap-кадра дополнительно разделены, поэтому 80 исходных состояний представлены 82 страницами PDF.
- Все 24 исходных употребления URL (11 уникальных адресов) и бесссылочная ссылка на Feller сохранены; на продолжениях источники повторяются. Все 11 уникальных изображений / 12 употреблений скопированы без изменений. Taxonomy использует общий `<TaxonomyDiagram denoising-diffusion />`, полученный из исходного TikZ с выделением DDPM.
- Содержание остаётся на английском. Исходных заметок `\note` в Beamer нет; добавленные технические заметки описывают только разбивку и раскрытия.
- Headmatter и оформление соответствуют утверждённой Lecture 1: 16:9, 1280×720, Arial/Georgia/Menlo, общая тема и адаптер макросов. Невидимая настройка `favicon: "data:,"` взята из существующих лекций 3/6, чтобы убрать внешний запрос к стандартному значку Slidev.
- Локальных переопределений шрифта, размера математики или масштаба нет. Формулы остаются редактируемыми; `multline` преобразованы в эквивалентные строки `aligned`. В числителе произведения Gaussian на слайде 28 применён `gathered`, все множители сохранены.

### Продолжения ради читаемости

| Исходный frame | Слайды Slidev | Разбиение |
|---|---|---|
| 4 | 4–5 | Perturbation / Assumption, затем DSM theorem и условный score; обе страницы статичны |
| 7 | 8–9 | Целые Training и Sampling; исходный статический recap сохранён |
| 10 | 13–14 | Markov kernel и Langevin, затем приближение и сопоставление параметров |
| 11 | 15–16 | Statement 1, затем все четыре шага вывода накопленного шума |
| 12 | 17–18 | Conditional kernels и рисунок, затем Statement 2 и условие стационарности |
| 14 | 20–21 | NCSN / Gaussian diffusion, затем DSM theorem и замечание |
| 17 | 25–26 | Gaussian reverse model, затем целые Forward / Reverse process и замечание |
| 18 | 27–28 | Bayes kernels, затем Gaussian product и все параметры |
| 23 | 35–36 | Conditioning, затем выделение t=1, Bayes substitution и telescoping |
| 24 | 37–38 | Log expansion, затем marginalization и полный переход к KL |

Все строки длинных выводов накапливаются, а не заменяют предыдущие; финальная раздатка включает промежуточную алгебру. Никакой редакторской темы или нового метода не добавлено.

## Проверки содержания

- **Notation / source:** проверены все 40 слайдов и исходные 26 frames; макросы согласованы с текущим `newcommands.tex`. `check` подтвердил 87 общих макросов, полное покрытие, карту, разделы, известные цитаты и 13 употреблений изображений вместе с taxonomy. Отдельно проверены URL в `\myfootnote{\href…}`, которые обычный checker не учитывает.
- **Формулы и раскрытия:** проверены forward Markov kernel, все шаги Statement 1, стационарность, сравнение NCSN / Gaussian diffusion, Bayes reverse kernel и условные Gaussian параметры, VAE factorization, все стадии ELBO: conditioning, t=1, Bayes substitution, telescoping, log expansion, marginalization и KL decomposition. Введённых потерь или изменений знаков/индексов не обнаружено.
- **Incoming Recap:** текущая Slidev L6 → Slidev L7; Langevin theorem, Fisher divergence, score function, DSM assumption/theorem, noise-level tradeoff, NCSN, Training и Sampling согласованы. Краткость recap и отсутствие рисунка ALD в recap сохранены из исходника.
- **Outgoing Recap:** Slidev L7 → Slidev L8, фактические recap sourceFrames 2–7 и их продолжения. Forward/reverse conditioning, `\alpha_t`, `\bar\alpha_t`, `\beta_t`, DSM identity, Gaussian posterior, distribution summary, latent-variable factorization, Standard ELBO и KL decomposition согласованы с L7 sourceFrames 10–25. Проверены знаки, пределы и источники; три повторённые recap-иллюстрации побайтно совпадают в L7/L8 Slidev и Beamer L8. Унаследованные `\bphi` при фиксированном encoder и `\text{const}(\btheta)` сохранены. Новых расхождений миграции нет.
- **Training / Sampling:** исходные ordered lists, названия, глаголы, формулы loss и двухиндексные состояния `\bx_t^l` сохранены. Блоки целые; в обычном теле L7 новых Training/Sampling не добавлено.
- **Summary:** шесть исходных статических тезисов; покрывают forward diffusion, DSM/Langevin, reverse process, Gaussian assumption, VAE и ELBO. Потерянных или новых тезисов нет.
- **Schedule:** четыре раздела и порядок совпадают со строкой `<b>Lecture 7:</b>` корневого README. Каталог Slidev содержит проверенные ссылки на slides.md, оба PDF и этот журнал.

## Сборка, PDF и браузер

- Node **24.19.0**, Slidev **52.19.1**, текущий lock-файл. Использована изолированная временная копия с физически отдельным `node_modules`: `/tmp/dgm-lecture7-migration-20260915/lectures-slidev`. Dev/build/export внутри неё выполнялись последовательно; серверы других задач не останавливались.
- `node tools/run.mjs finalize 7`: источник, **13 существующих тестов**, web build и оба PDF прошли. Генерация PDF проводилась только штатным Slidev exporter; исходные PDF не редактировались.
- Координатор проверил общие расширения taxonomy на ранее перенесённых L1–4: source/build/оба экспорта прошли, все 469 PDF-страниц сопоставлены с исходной рабочей редакцией. L1/L2/L4 попиксельно идентичны; в L3 изменился только случайный штрих существующего `v-mark` на 15 страницах, текст и пиксели вне его границ идентичны. Существующие PDF L1–4 не перезаписывались. Подробности регрессии — в [журнале L6](../lecture6/migration.md).
- `python tools/render-qa.py 7`: 82+40 страниц, сырых математических delimiters нет. Просмотрены **все 122 страницы** через контакты; крупно при ширине 1280 px — слайды 2, 5, 7, 9, 16, 20, 28, 31, 34, 36–39. Обрезок, наложений и дефектов математики не обнаружено. Финальные состояния всех 40 слайдов PDF с раскрытиями попиксельно равны соответствующим страницам раздатки.
- После изменения только favicon заново выполнены `finalize` и `render-qa.py`; **все 122 страницы попиксельно совпали** с просмотренной версией. Поэтому проведённая визуальная проверка полностью относится и к окончательным PDF.
- `node tools/inspect.mjs 7` после экспорта: все 40 слайдов / 82 состояния прошли; геометрия сохраняется, клики содержательны, обратные шаги совпадают, изображения загружены, ошибок KaTeX, сырой математики и пересечения границы источников нет. Единственный первоначальный заход текста в область источника на VAE-слайде исправлен высотой рисунка 125 px.
- Дополнительный сценарий проверил каждый доступный возврат с соседнего слайда к начальному и конечному состоянию всех 40 слайдов. После локальной правки favicon: **0 ошибок страницы, 0 HTTP failures, 0 внешних запросов**.
- На слайде 16 проверены мышью Pen, Undo/Redo, Save, Save & clear, Restore, четыре раскрытия и уход/возврат. Скачанный JSON содержит штрих, восстановление работает, SVG-координаты совпадают после возврата; синтетическая пометка после теста очищена. Toolbar/пометки отсутствуют в чистых PDF.
- QA-доказательства: `output/qa/lecture7/` внутри временной копии — контактные листы, текст, полноразмерные страницы, `browser-report.json`, `reentry-annotation-report.json`, `annotation-tools.png` и тестовые архивы. `reference/` рядом с временным проектом содержит сравнения L1/L6. Эти временные файлы не являются материалами для студентов.

### Фактическое сравнение стиля

При одинаковой ширине **1280 px**, в **PDF и браузере** просмотрены:

- L7 слайд 2 ↔ L6 слайд 15: Theorem (Informal), `\bx`, `\btheta`, gradient и noise в Langevin dynamics.
- L7 слайды 2/3 ↔ L6 слайд 19: Fisher divergence, score, ожидания, нормы, индексы и оптимизация.
- L7 слайд 9 ↔ L6 слайд 34: Sampling (Annealed Langevin Dynamics), `\bx_t^l`, `\bs_{\btheta,\sigma_t}`, `\bepsilon_t^l` и ordered list.
- L7 слайды 38/39 ↔ утверждённая L1 слайд 35: KL, ожидания, bold vectors/parameters, семейство KaTeX и оформление блоков.

В браузере отдельно подтверждена фактическая загрузка KaTeX-шрифтов. Вычисленные профили L1/L6/L7 совпадают: body Arial 24 px / 33.6 px, h1 37 px / 42.18 px, h2 24 px / 28.8 px, KaTeX 24.96 px / 29.952 px; цвета и начертания общие. Шрифт не подменялся локальным CSS. Для изолированных dev-сравнений использованы канонические пути `/private/tmp`, чтобы исключить несовпадение allow-list Vite для `/tmp`.

Исходники сравнения: L1 SHA-256 `e264d0b2af0ecaebd7be0a1fedf7549d28223afcd1f20b618d6c9052183c728e`; L6 SHA-256 `91c07db8a07fc7548cc60a4ce75ab881ead4ade5df13055075435abfae878ceb`. Выбранные страницы L6 повторно попиксельно сопоставлены с актуальным PDF после отдельного исправления taxonomy; эта правка не затронула сравниваемые блоки.

## Сохранённые особенности исходника

Редакторские формулировки не менялись в ходе переноса:

- `\text{const}(\btheta)` в DSM theorem сохранено как исходная запись, хотя рядом обсуждается минимизация по `\btheta`.
- Формулировка Feller theorem как Gaussian для sufficiently small `\beta_t` и ориентир `T≈1000` сохранены без усиления или уточнения.
- В Standard ELBO сохранены `\bphi` и максимум по `\bphi,\btheta`, хотя forward diffusion не имеет обучаемых параметров.
- Идеализированные конечные `\bx_T ∼ N(0,I)` и `\bx_0 ∼ p_data` в списках процессов сохранены; миграция не меняет исходные приближения.

Это унаследованные редакторские вопросы, а не введённые ошибки переноса.

## Ограничения проверки

Физическое перо, palm rejection, проектор и схема «планшет → экран» не проверялись. Браузерный тест мышью подтверждает только перечисленные сценарии общей панели. Presenter/viewer на разных устройствах отдельно не проверены; демонстраций с численными интерактивными параметрами в L7 нет. До занятия остаётся проверка реального оборудования по общему MIGRATION.md.

## Хеши локальных рисунков

Копии совпадают с `lectures/lecture7/figs/` побайтно.

| Файл | SHA-256 |
|---|---|
| `DDPM.png` | `847ccca8bbde0aaf9749d8d2a24ffd89aae4ebd59ec7847174527a805ac663d6` |
| `conditional_diffusion.png` | `a98939cb1c957b4340c591b229164a040d17ae595492c7441f6846daad45d970` |
| `diffusion_over_time.png` | `103b5ab5c1cba74015332ec605b46b93a7c7bd82e04720b9a243d217246aaa94` |
| `diffusion_pgm_forward.png` | `4a9d4c5c6d65bcc86909efe93addeb24b3a03b5c0ff4e8595c9b23bfe1d669e4` |
| `diffusion_pgm_reverse.png` | `ee70fd2f3dd4c00c28190064c31ef66f1cf4f9ae241ea55a4578b91c4b0b50d6` |
| `duoduo.jpg` | `867381828a71a3d9470019de55ebabd5a4c6872aabbedd2c5222bc5311a5a58f` |
| `inverse_distr_1d.png` | `f391d02cf7f9a910740ccd956b6a36056ef1dc52ec4d0fb9dfc202041d8007a2` |
| `multi_scale.jpg` | `051e1a8e3a1a1f678acfded26bfec1abb9b285fdca1d3a080fa5f249f95028e0` |
| `pitfalls.jpg` | `5e207ee7995d479ecc50eb8d40d442692597a9edaedbe915a3e50fb24e196ec4` |
| `single_noise.jpg` | `e7e3f02c506c222d42c123a6677efaf8ec6e89dc07bf0ee7daa91b7aa5b66878` |
| `smld.jpg` | `6ec30b4e08ba128585917926cdef4c7474bd3dd7e5d9f2c83554bcf6b98150cc` |
